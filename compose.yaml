services:
  omdb-proxy:
    # Builds from this checkout; compose.prod.yaml runs the published image.
    build: .
    restart: unless-stopped
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      - omdb-proxy-data:/data

volumes:
  omdb-proxy-data:
